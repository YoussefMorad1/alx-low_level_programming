#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <string.h>
#include "main.h"
/**
 * print_alphabet_x10 - main func
 * Description: hi hi hi
 * Return: 0
 */
int _isalpha(int c)
{
	if (c >= 'A' && c <= 'Z' || _islower(c))
		return (1);
	return (0);
}
